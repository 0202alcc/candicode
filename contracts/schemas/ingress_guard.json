{
  "type": "object",
  "additionalProperties": false,
  "required": ["sanitized", "tainted", "redactions"],
  "properties": {
    "sanitized": {"type": "boolean"},
    "tainted": {"type": "boolean"},
    "redactions": {"type": "array", "items": {"type": "string"}},
    "notes": {"type": "array", "items": {"type": "string"}}
  }
}
