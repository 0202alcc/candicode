[project]
name = "candicode"
version = "0.1.0"
description = "Pipeline extensions and native integration glue for custom OpenCode/CandiCode workflows."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = []
